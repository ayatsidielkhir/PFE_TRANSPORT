<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Facture <%= numero %></title>
  <style>
    body { font-family: Arial, sans-serif; margin: 40px; }
    h1 { text-align: center; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { border: 1px solid #ccc; padding: 8px; text-align: left; }
    th { background-color: #f5f5f5; }
    .totaux { margin-top: 30px; float: right; width: 300px; }
    .totaux td { border: none; }
    .footer { margin-top: 50px; text-align: right; font-size: 12px; }
  </style>
</head>
<body>
  <h1>Facture N° <%= numero %></h1>

  <p><strong>Date :</strong> <%= date %></p>
  <p><strong>Client :</strong> <%= client %></p>
  <p><strong>ICE :</strong> <%= ice %></p>
  <p><strong>Tracteur :</strong> <%= tracteur %></p>

  <% if (mode === 'auto') { %>
    <p style="color: #2a7; font-style: italic;">🛠️ Facture générée automatiquement à partir des trajets non facturés</p>
  <% } else { %>
    <p style="color: #888; font-style: italic;">✍️ Facture saisie manuellement</p>
  <% } %>

  <table>
    <thead>
      <tr>
        <th>Date</th>
        <th>Remorque</th>
        <th>Chargement</th>
        <th>Déchargement</th>
        <th>Total HT (DH)</th>
      </tr>
    </thead>
    <tbody>
      <% lignes.forEach(ligne => { %>
        <tr>
          <td><%= ligne.date %></td>
          <td><%= ligne.remorque %></td>
          <td><%= ligne.chargement %></td>
          <td><%= ligne.dechargement %></td>
          <td><%= ligne.totalHT.toFixed(2) %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <table class="totaux">
    <tr><td><strong>Total HT :</strong></td><td><%= totalHT.toFixed(2) %> DH</td></tr>
    <tr><td><strong>TVA :</strong></td><td><%= tva %> %</td></tr>
    <tr><td><strong>Total TTC :</strong></td><td><%= totalTTC.toFixed(2) %> DH</td></tr>
  </table>

  <div class="footer">
    <p>Document généré automatiquement par le système de gestion — <%= new Date().toLocaleDateString('fr-FR') %></p>
  </div>
</body>
</html>
